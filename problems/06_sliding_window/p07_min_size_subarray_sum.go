package sliding_window

// PROBLEM 7: Minimum Size Subarray Sum (LeetCode #209) — MEDIUM
// Find minimum length contiguous subarray with sum >= target. Return 0 if none.
// Example: target=7, nums=[2,3,1,2,4,3] → 2 ([4,3])
// Variable-size sliding window. Shrink when sum >= target.
// Target: O(n) time, O(1) space.

func MinSubArrayLen(target int, nums []int) int { return 0 }
