package binary_search

// PROBLEM 1: Search in Rotated Sorted Array (LeetCode #33) — MEDIUM
// Sorted array rotated at pivot. Find target index or -1.
// Example: [4,5,6,7,0,1,2], target=0 → 4
// Key: one half is always sorted. Check if target is in the sorted half.
// Target: O(log n) time, O(1) space.

func Search(nums []int, target int) int {
	return 0
}
