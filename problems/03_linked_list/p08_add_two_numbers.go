package linked_list

// PROBLEM 8: Add Two Numbers (LeetCode #2) — MEDIUM
// Two non-empty lists represent non-negative integers in REVERSE order.
// Add them and return the sum as a linked list.
// Example: (2→4→3) + (5→6→4) → 7→0→8  (342 + 465 = 807)
// Use a carry variable. Iterate while either list or carry is non-zero.
// Target: O(max(m,n)) time, O(max(m,n)) space.

func AddTwoNumbers(l1 *ListNode, l2 *ListNode) *ListNode {
	return nil
}
