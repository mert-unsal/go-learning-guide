package trees

// PROBLEM 2: Maximum Depth of Binary Tree (LeetCode #104) — EASY
// Return max depth (nodes along longest root-to-leaf path).
// depth = 1 + max(depth(left), depth(right)). Nil = 0.
// Target: O(n) time, O(h) space.

func MaxDepth(root *TreeNode) int {
	return 0
}
